class Cat {
    
    /**
     * 
     * @param {String} name the cat's name for the introduction
     * @param {String} altText alt text for cat photo
     * @param {String} photo the path to a photo of the cat
     */
    constructor(name, altText, photo) {
        this.name = name;
        this.altText = altText;
        this.photo = photo;        
    }

}